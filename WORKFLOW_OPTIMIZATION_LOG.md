# 工作流优化日志

> 本文档记录数据处理工作流的演进历史，包含被淘汰的设计方案及其优化理由。
> 当前目标工作流见 `DATA_PIPELINE.md`。

---

## 优化 1：实体管线简化（Phase 1 + Phase 3 移除）

**原始设计（5阶段）**

```
Phase 1  种子提取（游戏结构化数据）
  AvatarConfig + RogueAeonDisplay + AvatarBaseType
  → 102 个种子实体（携带 path/element/rarity 属性）

Phase 2  DeepSeek 丰富
  lore 文本 → 实体描述 + 别称 + 关系

Phase 3  合并
  种子实体 + DeepSeek 实体 → 按 canonical 去重合并
  种子字段优先级高于 DeepSeek

Phase 4  别称过滤（启发式规则）
  代词/泛化词 → context_aliases
  全局唯一别称 → aliases

Phase 5  序列化 → entities.json
```

**问题**
- Phase 1 生成的种子实体的「实体身份」完全被 Phase 2 覆盖（角色名也会从故事文本里被提取）
- Phase 3 合并逻辑复杂，且种子属性优先级判断容易出错
- Phase 4 启发式规则（代词/泛化词列表）准确率有限，不如 DeepSeek 理解上下文

**优化后设计（3阶段）**

```
Phase 1  上下文准备
  游戏属性表 → 作为 Phase 2 prompt 背景知识注入（不生成独立实体）

Phase 2  DeepSeek 一体化提取
  lore 文本 + 属性表背景 → 描述 + 属性 + 别称分类 + 关系（一次完成）

Phase 3  序列化 → entities.json
```

**优化理由**：游戏属性表的价值是提供属性补全依据，而不是作为独立实体存在。把它从「实体来源」改为「背景上下文」，让 DeepSeek 在提取时自然补全属性，消除合并步骤的复杂性。

---

## 优化 2：别称过滤从 Phase 4 移入 Phase 2

**原始设计**

Phase 4 使用启发式规则分类别称：
- 代词（他/她/祂）→ 删除
- 泛化名词（少年/将军/医生）→ context_aliases（约 200+ 条规则）
- 其余 → aliases

**问题**
- 规则无法覆盖所有泛化词，误分类率约 10-15%
- 启发式规则不理解文本上下文（「少年」在景元故事里是彦卿，在三月七故事里是丹恒）

**优化后设计**

在 Phase 2 的 prompt schema 中直接要求 DeepSeek 分类：

```
别称分类规则（由 DeepSeek 直接判断）：
  aliases → 全局唯一，任何地方出现都指向此实体（如「丹恒•腾荒」）
  context_aliases → 上下文相关（代词、泛化词、只在特定文段有效的称谓）
```

DeepSeek 有完整的上下文，能准确判断哪些别称是真正全局唯一的。

---

## 优化 3：实体去重简化（启发式候选对移除）

**原始设计（3步骤）**

```
Step 1  启发式候选生成（merge_candidates.json）
  Rule A: 别称 = 另一实体 canonical
  Rule B: 名称包含关系（同类型）
  Rule C: 名称编辑距离 ≤ 2（同类型）
  → 393 个候选对

Step 2  BGE-M3 嵌入相似度验证
  → 过滤假阳性

Step 3  DeepSeek 最终裁判
  → 人工审核 + 执行合并
```

**问题**

在实际执行中，我们将全部 3,473 个实体名称（~13k tokens）发给 DeepSeek，DeepSeek 直接返回了合并建议——这使得 Step 1（启发式候选生成）变得多余。只要实体名称总量在 DeepSeek 单次上下文范围内，可以直接让 DeepSeek 完成名称层去重。

**优化后设计（2步骤）**

```
步骤 1  名称层（DeepSeek 直接处理）
  全部实体名称 → DeepSeek 一次调用 → 直接得出合并建议
  无需启发式规则预筛选

步骤 2  语义层（BGE-M3 + DeepSeek，与向量化合并执行）
  BGE-M3 嵌入 → 600万对 → 高相似候选 → DeepSeek 验证
  用于发现名称看不出关系的语义相似实体（如「奥赫玛」≈「圣城」）
```

**保留 Step 2 的原因**：名称层 DeepSeek 只能发现名称上明显重复的实体，无法发现「不同名但指同一事物」的语义层重复。语义层需要嵌入向量才能规模化处理。

---

## 优化 4：wiki_mission 去重位置调整

**原始位置**：在实体生成之后执行

**问题**：去重是数据清洗步骤，在实体提取之前执行更合理——避免重复页面的内容被 DeepSeek 重复计入，影响 mention_count 和描述质量。

**优化后位置**：作为语料准备的一部分，在实体管线启动之前执行。

---

## 实际执行记录（当前成果）

当前 `output/entities.json` 由原始（未优化）管线生成，经过以下后处理：

1. 系列文档自动归并（44个子实体）
2. 名称层 DeepSeek 去重（42组合并）
3. 人工修正（黑塔/黑塔人偶拆分，米哈伊尔/钟表匠修正等）
4. 关系动词规范化（99条映射）

**结论**：当前成果可直接用于向量化和 LightRAG 建图阶段，无需按优化工作流重新生成。优化工作流用于**下次完整重建**（如游戏更新后）。
