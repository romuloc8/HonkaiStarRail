# 待解决问题与解决方案记录

> 最后更新：2026-05-14
> 状态：向量化完成（W07/W08 ✅），进入 LightRAG 建图阶段

---

## 待解决问题

### P1 — Entity-chunk 关联元数据（未执行）

**问题描述**

向量库的每个 chunk 缺少「哪些实体出现在这段文本里」的元数据，限制了：
- 实体过滤查询（「找所有提到银鬃铁卫的段落」）
- LightRAG 图检索和向量检索之间的连接

**解决方案**

chunking 时对每个 chunk 做字符串匹配（entities.json 的 canonical + aliases），将命中的实体加入 metadata：
```python
chunk_metadata["entities_mentioned"] = ["银鬃铁卫", "贝洛伯格", "杰帕德"]
```

**状态** 暂缓，不阻塞 LightRAG 建图

---

### P2 — 语义层实体去重（待执行，合并向量化）

**问题描述**

名称层 DeepSeek 去重已完成（42 组），但「奥赫玛」和「圣城」这类名称看不出关系、语义相似的实体仍未合并。

**解决方案**

1. BGE-M3 或 DashScope 对所有实体 description 生成嵌入
2. 余弦相似度找高相似候选对（压缩到数百对）
3. DeepSeek 逐对验证

**状态** 与下轮向量化合并执行

---

## 未来迭代问题

### 问题一：隐喻性文本的跨文档连接（暂不解决）

**问题描述**（2026-05-14 讨论）

**场景**：Document A 明确描述了实体 ABC 及其关系；Document B 用隐喻方式描述了同样的事件，但实体 ABC 及其所有别名均未出现在 Document B 中。当前系统能否连接这两个文档？

**当前能力边界**

| 情况 | Dense | BM25 | LightRAG 图 |
|------|-------|------|------------|
| 共享实体但不同描述 | ✅ | ✅ | ✅ |
| 语义相近的隐喻（词汇域相似）| ⚠️ 可能 | ❌ | ⚠️ 若共享节点 |
| 不同词汇域的隐喻，无共享实体 | ❌ | ❌ | ❌ |

**根本原因**：三种检索方式都依赖「可识别的信号」（共同词汇 / 相近语义 / 共享图节点）。纯隐喻性描述专门切断了表层信号，依靠读者的背景知识建立连接，而读者的背景知识不在检索系统的能力范围内。

**潜在解决方向**

**方向 A：语义注释层（Semantic Annotation）—— 推荐下次迭代**

在索引阶段，对高密度 lore 文本（书籍、遗器）用 DeepSeek 生成「规范化解释」，把明显的隐喻显式化：

```
原文（Document B）：「那年冬天，所有的火焰都熄灭了」
DeepSeek 注释：「此处可能指代某场导致文明消亡的灾变，时代背景为...可能关联建木灾异」
存储：原文 + 注释，两者共同参与 embedding
```

- 优点：不需要改变检索架构，只需索引时多一步 DeepSeek 调用
- 缺点：需要对所有 chunk 运行 DeepSeek（成本），且 DeepSeek 可能无法识别所有隐喻
- 适用范围：明显隐喻，不适用于完全novel的象征系统

**方向 B：HyDE（假设文档扩展）—— 查询端，零索引成本**

查询时先让 LLM 生成「如果有文档直接描述了这件事，它会怎么写」，然后用假设文档的向量搜索：

```
用户问：「关于射日传说的段落和哪段主线有关」
    ↓ LLM 生成假设文档：「帝弓司命弯弓射落穹桑，造翼者的星槎倾覆…」
    ↓ 用假设文档向量搜索 → 可能命中 Document A
```

- 优点：无需修改索引，只改查询端
- 缺点：LLM 必须已知连接才能生成假设文档（循环依赖）；对完全未知的连接无效

**方向 C：多跳迭代检索（Chain-of-Thought Retrieval）**

不做单次检索，做迭代式：检索 → 推理 → 扩展实体 → 再检索：

- 优点：能发现间接连接
- 缺点：延迟高（多次 LLM 调用），成本高

**方向 D：LightRAG Global Search（社区摘要）—— 当前架构最优解**

如果 Document A 和 Document B 的实体落在同一图社区（哪怕只是边缘节点），社区摘要会把主题综合起来，全局搜索可能触达：

- 优点：已在架构内，无需额外开发
- 缺点：依赖 LightRAG 社区检测质量；若两文档完全没有共同实体，仍然失效

---

### 问题二：涌现推断（Emergent Inference）—— 更深层的限制（暂不解决）

**问题描述**（2026-05-14 讨论，二相乐园案例）

**案例背景**

二相乐园主线揭示贪饕星神遗骸藏于其中，欢愉命途出现异常；模拟宇宙文本讲述月亮骑士联合多个实体击杀一个怪物（中毒的故事）。玩家推断：

```
幻月（二相乐园）        ≈  欢愉星神阿哈
月亮骑士（模拟宇宙文本） ≈  欢愉星神阿哈
模拟宇宙故事           =  古代星神击杀贪饕的历史寓言
欢愉命途异常           ←  贪饕遗骸的影响

最终猜想：阿哈曾联合其他星神击杀贪饕，贪饕遗骸留在乐园，
         其影响正在腐蚀欢愉命途。
```

**推理链分析**

| 步骤 | 推理内容 | 文本支持 | RAG 能否处理 |
|------|---------|---------|------------|
| 1 | 幻月 = 阿哈 | 无直接文本，符号系统认知 | ❌ |
| 2 | 月亮骑士 = 阿哈 | 无直接文本，跨文档符号一致性 | ❌ |
| 3 | 模拟宇宙故事 = 历史寓言 | 元叙事知识（游戏设计模式）| ❌ |
| 4 | 怪物 ≈ 贪饕 | 弱意象联想（贪婪/吞噬）| ⚠️ 弱 |
| 5 | 欢愉异常 ← 贪饕影响 | 无，纯因果推断 | ❌ |

**结论：当前架构无法推导出此类猜想。**

**根本性区别：两类隐秘关联**

| 类型 | 描述 | RAG 适用性 |
|------|------|-----------|
| **A 类：散落事实综合** | 信息在文本里，只是分散，需要跨文档检索和综合 | ✅ GraphRAG 设计目标 |
| **B 类：涌现推断** | 连接不在任何文本里，需要符号系统认知 + 元叙事知识 + 跨版本模式识别 | ❌ 超出 RAG 能力边界 |

玩家社区的「隐秘关联」猜想通常是 B 类——集体智识在大量阅读后对符号系统的涌现认知，是「知识生成」而非「信息检索」。

**玩家用到了两类我们语料库里没有的知识**：
1. **符号系统知识**：「月亮」在星铁中持续和阿哈绑定——不是任何单一文档的事实，是跨版本积累形成的
2. **元叙事知识**：「模拟宇宙叙事是历史事件的寓言」——关于游戏设计模式的元知识，不在任何游戏内文本里

**潜在解决方向**

**方向 A：索引玩家社区分析内容（高争议，需谨慎）**

将 HSR wiki 上的 lore 分析、玩家推理文章也纳入语料库。玩家已经显式写出了「幻月=阿哈」的推断，索引后 RAG 可以检索到。

- 优点：直接解决 B 类问题
- 缺点：混入了玩家推断（非官方 canon），检索结果的可信度下降；需要明确标注来源

**方向 B：注入符号系统先验（System Prompt 工程）**

将已知的符号绑定关系注入 LLM 的 system prompt：
```
「在星穹铁道中：月亮意象与欢愉星神阿哈持续绑定；模拟宇宙叙事通常是宇宙历史事件的寓言重演；」
```

- 优点：零数据成本，立刻有效
- 缺点：需要人工整理符号系统；随版本更新需要维护；注入的先验可能影响非相关查询

**方向 C：推测模式（Speculative Reasoning Mode）**

专门设计一个查询模式，LLM 被告知「以下是可能相关的证据，请推测可能的联系，并明确标注置信度」：

```json
{
  "mode": "speculative",
  "confidence_required": true,
  "response_format": "假说 + 证据 + 置信度 + 反驳可能"
}
```

- 优点：透明的不确定性；用户知道是猜测
- 缺点：LLM 可能产生幻觉；需要专门设计 prompt 和输出格式

**方向 D：不解决，明确边界**

在系统 prompt 中明确告知用户：「当前系统能够综合游戏内明确文本，但无法推导依赖玩家社区共识的隐性符号关联。」

这是当前最诚实的处理方式。

**建议下次迭代**：
- 如果目标是覆盖「玩家级推断」，方向 A（索引社区分析）+ 方向 B（符号先验注入）是成本最低的组合
- 方向 C（推测模式）是最透明的长期解决方案，但需要更多工程投入

### 时间建模精度（已深入讨论，暂不解决）

**问题来源**（2026-05-14 讨论）

当前时间系统（15 个命名锚点 + `before/during/after/spanning`）颗粒度不足，在涉及时间的 Hard 题上容易产生错误推理。

**问题的多个维度**

**维度 1：历法多样性**
- 琥珀纪（第N纪）：仙舟联盟历法，用于仙舟历史
- 光历XXXX年：翁法罗斯历法，精确到年份
- 相对时间（「二十年前」「数百年前」）：参照物不明确，仙舟长生种的「二十年前」与贝洛伯格人的「二十年前」含义完全不同
- 当前锚点（`arc_main_belobog` 等）：游戏弧线级别，粒度太粗

**维度 2：时间框架嵌套（翁法罗斯问题）**
- 翁法罗斯位于帝皇权杖内部的**模拟宇宙**，内部时间流速与外部不等价
- 翁法罗斯的历史涉及**多次轮回**，同一事件（逐火之旅、凯撒崛起）在每次轮回里重演，不同轮回下同一实体可能处于不同状态
- 例：「凯撒在第几次轮回里成功」是合理的问题，但当前时间模型无法表达「轮回内的时间」

**维度 3：精度层级差异**

| 表述类型 | 示例 | 当前模型的处理 |
|---------|------|-------------|
| 精确时间点 | 「光历3910年底」 | 只能归入最近锚点，精度丢失 |
| 大致时代 | 「某个琥珀纪」「数百年前」 | 和精确时间点用同一字段，无法区分 |
| 时间区间 | 「建木灾祸至饮月之乱之间」| position 字段无法表达区间 |
| 持续时长 | 「封闭了十余年」| 完全无法表达 |

**维度 4：信息可信度差异**

| 信息来源 | 示例 | 可信度 |
|---------|------|--------|
| 游戏内正史记录 | 《六御审饮月》 | 高（游戏内设定） |
| 仙舟民间传说 | 「据说帝弓司命…」| 中（可能有艺术加工）|
| 书籍作者观点 | 某学者撰写的史书 | 中（有立场和局限）|
| 神话/传说 | 遗器描述的上古故事 | 低（高度神话化）|

当前所有关系都被平等对待，不区分可信度。

---

**潜在解决方向**

**方向 A：加 `reliability` 字段（高性价比，建议下次迭代优先做）**

对每条关系增加可信度标注：
```json
"temporal": {
  "anchor": "event_yinyue",
  "position": "before",
  "reliability": "confirmed"  // confirmed | legend | speculation
}
```

优点：改动小，对 Hard 题推理质量帮助最大（LLM 可以区分「传说中」和「确认发生」）
缺点：DeepSeek 提取时判断可信度需要更复杂的 prompt，可能有误判

**方向 B：加 `raw_evidence` 字段（高性价比，建议下次迭代优先做）**

同时保存原文时间表述，不依赖归一化结果：
```json
"temporal": {
  "anchor": "event_yinyue",
  "position": "after",
  "raw_evidence": "饮月之乱平定后三百余琥珀纪"
}
```

优点：零额外成本（extraction 时顺带提取），查询时 LLM 直接读原文做更准确的时间推理
缺点：`raw_evidence` 是自然语言，程序化查询（「找 anchor=event_yinyue 且发生在 300 年后的关系」）无法利用

**方向 C：多维度时间对象（完整方案，成本高）**

```json
"temporal": {
  "calendar_system": "xianzhou_amber | amphorean_light | relative | unknown",
  "anchor": "event_yinyue",
  "position": "after",
  "approximate_distance": "centuries",
  "precision": "exact | approximate | range | vague",
  "reliability": "confirmed | legend | speculation",
  "simulation_layer": null,           // null = 外部时间；"amphorean_scepter" = 权杖内
  "cycle_context": null               // null = 外部；整数 N = 第 N 次轮回
}
```

优点：完整建模，支持精细查询
缺点：字段多，DeepSeek 难以准确填写（容易产生幻觉），需要大量人工校验

**方向 D：翁法罗斯轮回专项标注（窄场景，中成本）**

只针对翁法罗斯相关实体添加轮回上下文，其他实体不变：
```json
// 仅翁法罗斯角色/事件
"temporal_context": {
  "frame": "amphorean_scepter",
  "cycle_number": 7,
  "notes": "此状态仅在第7次轮回中有效，轮回重置后状态变化"
}
```

优点：不影响其他实体，集中解决最复杂的问题
缺点：需要人工标注「哪些实体属于翁法罗斯体系」

**方向 E：放弃时间归一化，只存原文（最简单）**

完全不尝试归一化，只存 `raw_evidence`，全部交给查询时 LLM 推理：
```json
"temporal": {
  "raw_text": "饮月之乱平定后三百余琥珀纪",
  "reliability": "confirmed"
}
```

优点：零成本，天然适应所有时间表述（包括轮回、模拟时间）
缺点：无法做程序化时间过滤，依赖 LLM 推理能力，结果可能不稳定

---

**不建议做的**

强行将所有历法统一到一个绝对时间轴。游戏本身刻意模糊了不同历法之间的转换关系（琥珀纪时长不固定，翁法罗斯轮回次数不明），强行归一化会产生**虚假精度**——看起来精确，实际上是错误的。

---

**建议下次迭代执行顺序**

1. **方向 B（raw_evidence）+ 方向 A（reliability）** — 改动小，收益大，两个字段在下次 entity_pipeline 重跑时顺带加入即可
2. **方向 D（翁法罗斯轮回标注）** — 如果 Hard 题里有明显的翁法罗斯时间相关错误，再做

---

## 已解决问题（归档）

以下问题已在数据工程阶段完成，不再需要跟踪：

| 问题 | 解决方案 | 完成时间 |
|------|---------|---------|
| 关系动词不规范 | 提取三元组 → DeepSeek 分组 → normalization_map.json | 2026-05-12 |
| 实体合并候选生成 | 全部名称发 DeepSeek 一次调用直接得出 | 2026-05-12 |
| wiki_mission 重复内容 | 按 wiki_url 去重（299→199 条）| 2026-05-12 |
| 现有 JSONL 未经新清洗规则 | 重跑清洗层，3468+744 处修正 | 2026-05-12 |
| Embedding 模型选型 | DashScope text-embedding-v4（国际版）| 2026-05-13 |
| 向量库选型 | Chroma 本地持久化 | 2026-05-13 |
| Chunking 策略 | 对话场景直接使用；lore 按段落切分 | 2026-05-13 |
| Dense 向量索引 | 7,819 chunks 完成索引（W08）| 2026-05-13 |
| Sparse 向量索引 | BM25 + 字符 bigram，HybridRetriever（RRF）| 2026-05-13 |
| 对话数据颗粒度优化 | 重新爬取，wiki 场景级（2,564 场景），按章节分文件 | 2026-05-13 |
| 文件结构规范化 | output/main_story/ companion/ continuance/ adventure/ activity/ lore/ | 2026-05-13 |
