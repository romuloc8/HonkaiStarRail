# 待解决问题与解决方案记录

> 最后更新：2026-05-14
> 状态：向量化完成（W07/W08 ✅），进入 LightRAG 建图阶段

---

## 待解决问题

### P1 — Entity-chunk 关联元数据（未执行）

**问题描述**

向量库的每个 chunk 缺少「哪些实体出现在这段文本里」的元数据，限制了：
- 实体过滤查询（「找所有提到银鬃铁卫的段落」）
- LightRAG 图检索和向量检索之间的连接

**解决方案**

chunking 时对每个 chunk 做字符串匹配（entities.json 的 canonical + aliases），将命中的实体加入 metadata：
```python
chunk_metadata["entities_mentioned"] = ["银鬃铁卫", "贝洛伯格", "杰帕德"]
```

**状态** 暂缓，不阻塞 LightRAG 建图

---

### P2 — 语义层实体去重（待执行，合并向量化）

**问题描述**

名称层 DeepSeek 去重已完成（42 组），但「奥赫玛」和「圣城」这类名称看不出关系、语义相似的实体仍未合并。

**解决方案**

1. BGE-M3 或 DashScope 对所有实体 description 生成嵌入
2. 余弦相似度找高相似候选对（压缩到数百对）
3. DeepSeek 逐对验证

**状态** 与下轮向量化合并执行

---

## 未来迭代问题

### 问题一：隐喻性文本的跨文档连接（暂不解决）

**问题描述**（2026-05-14 讨论）

**场景**：Document A 明确描述了实体 ABC 及其关系；Document B 用隐喻方式描述了同样的事件，但实体 ABC 及其所有别名均未出现在 Document B 中。当前系统能否连接这两个文档？

**当前能力边界**

| 情况 | Dense | BM25 | LightRAG 图 |
|------|-------|------|------------|
| 共享实体但不同描述 | ✅ | ✅ | ✅ |
| 语义相近的隐喻（词汇域相似）| ⚠️ 可能 | ❌ | ⚠️ 若共享节点 |
| 不同词汇域的隐喻，无共享实体 | ❌ | ❌ | ❌ |

**根本原因**：三种检索方式都依赖「可识别的信号」（共同词汇 / 相近语义 / 共享图节点）。纯隐喻性描述专门切断了表层信号，依靠读者的背景知识建立连接，而读者的背景知识不在检索系统的能力范围内。

**潜在解决方向**

**方向 A：语义注释层（Semantic Annotation）—— 推荐下次迭代**

在索引阶段，对高密度 lore 文本（书籍、遗器）用 DeepSeek 生成「规范化解释」，把明显的隐喻显式化：

```
原文（Document B）：「那年冬天，所有的火焰都熄灭了」
DeepSeek 注释：「此处可能指代某场导致文明消亡的灾变，时代背景为...可能关联建木灾异」
存储：原文 + 注释，两者共同参与 embedding
```

- 优点：不需要改变检索架构，只需索引时多一步 DeepSeek 调用
- 缺点：需要对所有 chunk 运行 DeepSeek（成本），且 DeepSeek 可能无法识别所有隐喻
- 适用范围：明显隐喻，不适用于完全novel的象征系统

**方向 B：HyDE（假设文档扩展）—— 查询端，零索引成本**

查询时先让 LLM 生成「如果有文档直接描述了这件事，它会怎么写」，然后用假设文档的向量搜索：

```
用户问：「关于射日传说的段落和哪段主线有关」
    ↓ LLM 生成假设文档：「帝弓司命弯弓射落穹桑，造翼者的星槎倾覆…」
    ↓ 用假设文档向量搜索 → 可能命中 Document A
```

- 优点：无需修改索引，只改查询端
- 缺点：LLM 必须已知连接才能生成假设文档（循环依赖）；对完全未知的连接无效

**方向 C：多跳迭代检索（Chain-of-Thought Retrieval）**

不做单次检索，做迭代式：检索 → 推理 → 扩展实体 → 再检索：

- 优点：能发现间接连接
- 缺点：延迟高（多次 LLM 调用），成本高

**方向 D：LightRAG Global Search（社区摘要）—— 当前架构最优解**

如果 Document A 和 Document B 的实体落在同一图社区（哪怕只是边缘节点），社区摘要会把主题综合起来，全局搜索可能触达：

- 优点：已在架构内，无需额外开发
- 缺点：依赖 LightRAG 社区检测质量；若两文档完全没有共同实体，仍然失效

---

### 问题二：涌现推断（Emergent Inference）—— 更深层的限制（暂不解决）

**问题描述**（2026-05-14 讨论，二相乐园案例）

**案例背景**

二相乐园主线揭示贪饕星神遗骸藏于其中，欢愉命途出现异常；模拟宇宙文本讲述月亮骑士联合多个实体击杀一个怪物（中毒的故事）。玩家推断：

```
幻月（二相乐园）        ≈  欢愉星神阿哈
月亮骑士（模拟宇宙文本） ≈  欢愉星神阿哈
模拟宇宙故事           =  古代星神击杀贪饕的历史寓言
欢愉命途异常           ←  贪饕遗骸的影响

最终猜想：阿哈曾联合其他星神击杀贪饕，贪饕遗骸留在乐园，
         其影响正在腐蚀欢愉命途。
```

**推理链分析**

| 步骤 | 推理内容 | 文本支持 | RAG 能否处理 |
|------|---------|---------|------------|
| 1 | 幻月 = 阿哈 | 无直接文本，符号系统认知 | ❌ |
| 2 | 月亮骑士 = 阿哈 | 无直接文本，跨文档符号一致性 | ❌ |
| 3 | 模拟宇宙故事 = 历史寓言 | 元叙事知识（游戏设计模式）| ❌ |
| 4 | 怪物 ≈ 贪饕 | 弱意象联想（贪婪/吞噬）| ⚠️ 弱 |
| 5 | 欢愉异常 ← 贪饕影响 | 无，纯因果推断 | ❌ |

**结论：当前架构无法推导出此类猜想。**

**根本性区别：两类隐秘关联**

| 类型 | 描述 | RAG 适用性 |
|------|------|-----------|
| **A 类：散落事实综合** | 信息在文本里，只是分散，需要跨文档检索和综合 | ✅ GraphRAG 设计目标 |
| **B 类：涌现推断** | 连接不在任何文本里，需要符号系统认知 + 元叙事知识 + 跨版本模式识别 | ❌ 超出 RAG 能力边界 |

玩家社区的「隐秘关联」猜想通常是 B 类——集体智识在大量阅读后对符号系统的涌现认知，是「知识生成」而非「信息检索」。

**玩家用到了两类我们语料库里没有的知识**：
1. **符号系统知识**：「月亮」在星铁中持续和阿哈绑定——不是任何单一文档的事实，是跨版本积累形成的
2. **元叙事知识**：「模拟宇宙叙事是历史事件的寓言」——关于游戏设计模式的元知识，不在任何游戏内文本里

**潜在解决方向**

**方向 A：索引玩家社区分析内容（高争议，需谨慎）**

将 HSR wiki 上的 lore 分析、玩家推理文章也纳入语料库。玩家已经显式写出了「幻月=阿哈」的推断，索引后 RAG 可以检索到。

- 优点：直接解决 B 类问题
- 缺点：混入了玩家推断（非官方 canon），检索结果的可信度下降；需要明确标注来源

**方向 B：注入符号系统先验（System Prompt 工程）**

将已知的符号绑定关系注入 LLM 的 system prompt：
```
「在星穹铁道中：月亮意象与欢愉星神阿哈持续绑定；模拟宇宙叙事通常是宇宙历史事件的寓言重演；」
```

- 优点：零数据成本，立刻有效
- 缺点：需要人工整理符号系统；随版本更新需要维护；注入的先验可能影响非相关查询

**方向 C：推测模式（Speculative Reasoning Mode）**

专门设计一个查询模式，LLM 被告知「以下是可能相关的证据，请推测可能的联系，并明确标注置信度」：

```json
{
  "mode": "speculative",
  "confidence_required": true,
  "response_format": "假说 + 证据 + 置信度 + 反驳可能"
}
```

- 优点：透明的不确定性；用户知道是猜测
- 缺点：LLM 可能产生幻觉；需要专门设计 prompt 和输出格式

**方向 D：不解决，明确边界**

在系统 prompt 中明确告知用户：「当前系统能够综合游戏内明确文本，但无法推导依赖玩家社区共识的隐性符号关联。」

这是当前最诚实的处理方式。

**建议下次迭代**：
- 如果目标是覆盖「玩家级推断」，方向 A（索引社区分析）+ 方向 B（符号先验注入）是成本最低的组合
- 方向 C（推测模式）是最透明的长期解决方案，但需要更多工程投入

### 时间建模精度（已深入讨论，暂不解决）

**问题来源**（2026-05-14 讨论）

当前时间系统（15 个命名锚点 + `before/during/after/spanning`）颗粒度不足，在涉及时间的 Hard 题上容易产生错误推理。

**问题的多个维度**

**维度 1：历法多样性**
- 琥珀纪（第N纪）：仙舟联盟历法，用于仙舟历史
- 光历XXXX年：翁法罗斯历法，精确到年份
- 相对时间（「二十年前」「数百年前」）：参照物不明确，仙舟长生种的「二十年前」与贝洛伯格人的「二十年前」含义完全不同
- 当前锚点（`arc_main_belobog` 等）：游戏弧线级别，粒度太粗

**维度 2：时间框架嵌套（翁法罗斯问题）**
- 翁法罗斯位于帝皇权杖内部的**模拟宇宙**，内部时间流速与外部不等价
- 翁法罗斯的历史涉及**多次轮回**，同一事件（逐火之旅、凯撒崛起）在每次轮回里重演，不同轮回下同一实体可能处于不同状态
- 例：「凯撒在第几次轮回里成功」是合理的问题，但当前时间模型无法表达「轮回内的时间」

**维度 3：精度层级差异**

| 表述类型 | 示例 | 当前模型的处理 |
|---------|------|-------------|
| 精确时间点 | 「光历3910年底」 | 只能归入最近锚点，精度丢失 |
| 大致时代 | 「某个琥珀纪」「数百年前」 | 和精确时间点用同一字段，无法区分 |
| 时间区间 | 「建木灾祸至饮月之乱之间」| position 字段无法表达区间 |
| 持续时长 | 「封闭了十余年」| 完全无法表达 |

**维度 4：信息可信度差异**

| 信息来源 | 示例 | 可信度 |
|---------|------|--------|
| 游戏内正史记录 | 《六御审饮月》 | 高（游戏内设定） |
| 仙舟民间传说 | 「据说帝弓司命…」| 中（可能有艺术加工）|
| 书籍作者观点 | 某学者撰写的史书 | 中（有立场和局限）|
| 神话/传说 | 遗器描述的上古故事 | 低（高度神话化）|

当前所有关系都被平等对待，不区分可信度。

---

**潜在解决方向**

**方向 A：加 `reliability` 字段（高性价比，建议下次迭代优先做）**

对每条关系增加可信度标注：
```json
"temporal": {
  "anchor": "event_yinyue",
  "position": "before",
  "reliability": "confirmed"  // confirmed | legend | speculation
}
```

优点：改动小，对 Hard 题推理质量帮助最大（LLM 可以区分「传说中」和「确认发生」）
缺点：DeepSeek 提取时判断可信度需要更复杂的 prompt，可能有误判

**方向 B：加 `raw_evidence` 字段（高性价比，建议下次迭代优先做）**

同时保存原文时间表述，不依赖归一化结果：
```json
"temporal": {
  "anchor": "event_yinyue",
  "position": "after",
  "raw_evidence": "饮月之乱平定后三百余琥珀纪"
}
```

优点：零额外成本（extraction 时顺带提取），查询时 LLM 直接读原文做更准确的时间推理
缺点：`raw_evidence` 是自然语言，程序化查询（「找 anchor=event_yinyue 且发生在 300 年后的关系」）无法利用

**方向 C：多维度时间对象（完整方案，成本高）**

```json
"temporal": {
  "calendar_system": "xianzhou_amber | amphorean_light | relative | unknown",
  "anchor": "event_yinyue",
  "position": "after",
  "approximate_distance": "centuries",
  "precision": "exact | approximate | range | vague",
  "reliability": "confirmed | legend | speculation",
  "simulation_layer": null,           // null = 外部时间；"amphorean_scepter" = 权杖内
  "cycle_context": null               // null = 外部；整数 N = 第 N 次轮回
}
```

优点：完整建模，支持精细查询
缺点：字段多，DeepSeek 难以准确填写（容易产生幻觉），需要大量人工校验

**方向 D：翁法罗斯轮回专项标注（窄场景，中成本）**

只针对翁法罗斯相关实体添加轮回上下文，其他实体不变：
```json
// 仅翁法罗斯角色/事件
"temporal_context": {
  "frame": "amphorean_scepter",
  "cycle_number": 7,
  "notes": "此状态仅在第7次轮回中有效，轮回重置后状态变化"
}
```

优点：不影响其他实体，集中解决最复杂的问题
缺点：需要人工标注「哪些实体属于翁法罗斯体系」

**方向 E：放弃时间归一化，只存原文（最简单）**

完全不尝试归一化，只存 `raw_evidence`，全部交给查询时 LLM 推理：
```json
"temporal": {
  "raw_text": "饮月之乱平定后三百余琥珀纪",
  "reliability": "confirmed"
}
```

优点：零成本，天然适应所有时间表述（包括轮回、模拟时间）
缺点：无法做程序化时间过滤，依赖 LLM 推理能力，结果可能不稳定

---

**不建议做的**

强行将所有历法统一到一个绝对时间轴。游戏本身刻意模糊了不同历法之间的转换关系（琥珀纪时长不固定，翁法罗斯轮回次数不明），强行归一化会产生**虚假精度**——看起来精确，实际上是错误的。

---

**建议下次迭代执行顺序**

1. **方向 B（raw_evidence）+ 方向 A（reliability）** — 改动小，收益大，两个字段在下次 entity_pipeline 重跑时顺带加入即可
2. **方向 D（翁法罗斯轮回标注）** — 如果 Hard 题里有明显的翁法罗斯时间相关错误，再做

---

## 已解决问题（归档）

以下问题已在数据工程阶段完成，不再需要跟踪：

| 问题 | 解决方案 | 完成时间 |
|------|---------|---------|
| 关系动词不规范 | 提取三元组 → DeepSeek 分组 → normalization_map.json | 2026-05-12 |
| 实体合并候选生成 | 全部名称发 DeepSeek 一次调用直接得出 | 2026-05-12 |
| wiki_mission 重复内容 | 按 wiki_url 去重（299→199 条）| 2026-05-12 |
| 现有 JSONL 未经新清洗规则 | 重跑清洗层，3468+744 处修正 | 2026-05-12 |
| Embedding 模型选型 | DashScope text-embedding-v4（国际版）| 2026-05-13 |
| 向量库选型 | Chroma 本地持久化 | 2026-05-13 |
| Chunking 策略 | 对话场景直接使用；lore 按段落切分 | 2026-05-13 |
| Dense 向量索引 | 7,819 chunks 完成索引（W08）| 2026-05-13 |
| Sparse 向量索引 | BM25 + 字符 bigram，HybridRetriever（RRF）| 2026-05-13 |
| 对话数据颗粒度优化 | 重新爬取，wiki 场景级（2,564 场景），按章节分文件 | 2026-05-13 |
| 文件结构规范化 | output/main_story/ companion/ continuance/ adventure/ activity/ lore/ | 2026-05-13 |

---

## 第二次深度分析：Schema 盲区全面审计

> 记录时间：2026-06-05
> 来源：多轮讨论 + 数据核查（entities.json + 对话提取器源码 + 游戏数据审计）

---

### 一、对话内容层面

#### 1.1 玩家选项分支内容——当前最严重的数据质量问题

**现状**：`wiki_mission.py` 中 `_expand_plot_options` 对 `{{剧情选项}}` 的处理：
- 所有分支 NPC 回应相同 → 合并为一条（正确）
- 分支 NPC 回应不同 → 所有分支全部保留，并列索引

**问题**：全部保留意味着某个分支里才成立的事实和另一个分支里相反的叙述被同等索引。系统无法知道哪个分支"发生了"，导致：
- 同一问题可能检索到互相矛盾的事实
- 玩家自己说的选项台词（"开拓者：选项1 / 选项2"）被当作事实性陈述处理

**缺失字段**：
```json
{
  "dialogue_entry": {
    "is_player_choice": true,
    "branch_id": "option_a",
    "all_branches_equivalent": false,
    "canonical_branch": null  // 游戏设计本身没有"正确选项"
  }
}
```

**影响**：目前所有含剧情选项的场景（覆盖全部主线）的对话数据都有此问题。

---

#### 1.2 开拓者/玩家角色的话语不应被视为世界观事实

开拓者所说的话部分由玩家控制，不代表世界观内的"客观陈述"。大丽花修改记忆是极端案例，但一般情况下：
- 开拓者的台词 = 玩家在特定情境下的输入，不是 NPC 的客观陈述
- 开拓者对自身经历的描述可能不完整（失忆背景）

**缺失标注**：对话数据中应标记 `speaker_is_player_character: true`，这类陈述的可信度天然低于 NPC 陈述。

---

#### 1.3 引用链（Quote Attribution Chain）

许多重要事实通过引用链传递：
> 「据《建木史话》记载，文献中提到某学者认为……」

每经过一层引用，可信度降低。当前系统只提取最终陈述，丢失了引用链。

```json
{
  "attribution_chain": [
    {"source": "《建木史话》", "type": "historical_record"},
    {"source": "某仙舟学者", "type": "character_account"},
    {"source": "当前对话NPC", "type": "hearsay"}
  ],
  "effective_reliability": "legend"  // 取链中最低可信度
}
```

---

### 二、实体本体论层面

#### 2.1 命途（Path）与星神（Aeon）是独立本体——当前存在根本性错误

**当前**：`巡猎` Path 实体和 `岚（巡猎星神）` Aeon 实体是分开的，但 Path 的关联星神字段大量为空（13个Path实体中有8个无关联星神）。

**根本性问题**：命途在星神陨落后依然存在。`繁育` 命途在 `塔伊兹育罗斯` 被击杀后仍然运作。命途不属于星神，星神是命途的"当前代言人"。这是一个本体论错误：

```
错误理解：命途 = 星神的属性
正确关系：命途 → 独立存在 → 星神是命途的现任执掌者（valid_from/until）
```

**缺失**：
```json
{
  "entity": "繁育",
  "type": "path",
  "current_aeon": null,      // 塔伊兹育罗斯死后空缺
  "former_aeons": [
    {"canonical": "塔伊兹育罗斯", "valid_until_event": "寰宇蝗灾终结"}
  ],
  "path_exists_independently": true
}
```

---

#### 2.2 角色发展弧（Character Arc）——静态实体无法表达成长

当前实体是静态快照，但角色在故事中有明显的状态演变：
- 丹恒：从疏离冷淡 → 逐渐敞开心扉（ch04→ch08 间）
- 镜流：从表面对立 → 与开拓者达成默契（仙舟弧内部）
- 刃：从只有复仇执念 → 逐渐复苏人性（ch07~ch08）

**缺失**：
```json
{
  "character_arc": {
    "arc_start_state": "疏离冷漠，拒绝承认前世",
    "arc_end_state": "接受过去，允许开拓者知晓饮月往事",
    "arc_span": {"from": "ch04", "to": "ch08"},
    "key_turning_points": [
      {"mission": "ch07_某场景", "change": "第一次主动谈及饮月君"}
    ]
  }
}
```

这类信息对回答「X 在 Y 时期的心理状态如何」至关重要。

---

#### 2.3 宇宙地理结构（Cosmological Ontology）——完全缺失

星铁的宇宙有多层本体结构，当前完全未建模：

| 层次 | 内容 | 当前状态 |
|------|------|---------|
| 物质宇宙 | 星球/星系组成的真实宇宙 | 部分有地点实体 |
| 模拟宇宙 | 黑塔研究项目，模拟星神的计算空间 | 无专项建模 |
| 虚空 | 虚无命途的领域，虚空侍者的来源 | 无 |
| 冥途 | 某些亡者进入的领域（白露/忆庭相关）| 无 |
| 帝皇权杖内部 | 翁法罗斯所在的模拟宇宙，独立时间流 | 仅 `simulation_context` 字段，无内容 |

这些"地点"不是普通地点，是本体论层级的区分，影响「在A地发生的事是否等同于在B地」的推理。

---

### 三、关系语义层面

#### 3.1 叙述视角（Narrative Perspective）——同一事件，不同代理人

同一个关系从不同代理人视角有完全不同的含义：

```
「幻胧对仙舟的行动」:
  - 幻胧视角: "我在执行毁灭星神的使命"   → relation: executes_mission
  - 仙舟视角: "幻胧在破坏我们的家园"     → relation: attacked
  - 客观视角: "幻胧以建木灾异为手段，试图毁灭仙舟" → relation: attempted_destruction
```

**缺失**：
```json
{
  "relation": "attacked",
  "perspective": "victim",  // agent | victim | observer | omniscient
  "alternative_framings": [
    {"perspective": "agent", "relation": "executes_mission", "reliability": "character_account"}
  ]
}
```

---

#### 3.2 关系规模/量级（Relation Magnitude）——无量纲区分

「纳努克 opposes 药师」（宇宙级，两个星神之间）与「开拓者 opposes 某个小反派」使用同一个 `opposes` 动词，但量级相差宇宙到个人。

**缺失**：
```json
{
  "relation": "opposes",
  "magnitude": "cosmic",  // personal | regional | planetary | cosmic
  "domain": "ideological"  // military | political | ideological | personal
}
```

---

#### 3.3 跨章节前后呼应（Foreshadowing / Callback）——最重要的隐性连接

这正是我们整个项目要找的"隐秘联系"，但 schema 中完全没有对应结构。

**典型案例**：
- ch01 里三月七被从六相冰中打捞，「六相冰」来源成谜 → ch07+后续揭示「记忆星神」线索
- ch04 里建木上出现异常 → ch06 揭示是幻胧操控
- 遗器「苍穹战线格拉默」描述的帝国灾亡 → 流萤角色故事里得知她是这段历史的幸存者

**缺失**：
```json
{
  "cross_reference_pairs": [
    {
      "hint_source": {"chapter": "ch01", "scene": "三月七苏醒"},
      "hint_content": "六相冰的神秘来源",
      "revelation_source": {"chapter": "ch07+", "lore": "记忆星神浮黎"},
      "connection_type": "foreshadowing",  // foreshadowing | callback | contradiction_resolution | identity_reveal
      "confidence": "probable"
    }
  ]
}
```

**这是整个 RAG 项目核心价值所在，却完全没有 schema 支撑。**

---

### 四、时间模型层面

#### 4.1 实体知识的时效性（What An Entity Knows At Time T）

我们记录了"某事在时间T成立"，但没有记录"角色X在时间T是否知道这件事"。

**典型问题**：
- 回答「在仙舟主线期间，景元是否知道刃曾是云上五骁」→ 需要知识时效性
- 回答「三月七初见丹恒时，她是否知道他是前任龙尊的转世」→ 不知道

```json
{
  "knowledge_timeline": {
    "entity": "景元",
    "knows": {
      "「刃是应星」": {"known_since": "ch07", "reliability": "confirmed"},
      "「白露是白珩转世」": {"known_since": null, "status": "unknown"}
    }
  }
}
```

#### 4.2 "被遗忘"也是一种时间事件

三月七失忆、开拓者被大丽花修改记忆、某些仙舟历史被刻意删除——"遗忘"本身是一个时序事件，和任何其他状态变化一样重要。

```json
{
  "memory_events": [
    {
      "entity": "三月七",
      "event_type": "memory_loss",  // memory_loss | memory_modification | memory_recovery
      "scope": "所有主线前记忆",
      "known_cause": null,
      "temporal": {"anchor": "epoch_unknown", "position": "before"}
    }
  ]
}
```

---

### 五、来源覆盖层面

#### 5.1 对话选项导致的内容缺口（已确认：0% 分支标注）

核查结果：`has_branches` 场景数 = **0**（8630 个场景无任何分支标注）。这不是因为对话没有分支，而是当前提取器把分支合并或展开后不保留原始标注。所有含 `{{剧情选项}}` 的场景的分支信息已丢失。

#### 5.2 成就描述——最被忽视的隐性 lore 来源

成就标题和描述中包含大量隐性世界观信息，例如：
- 成就「以牙还牙」描述了某段不为人知的历史
- 成就「真正的幸存者」暗示了某角色的隐藏故事线

当前完全未索引。获取成本极低（静态 JSON），信息密度高。

#### 5.3 模拟宇宙战斗故事（Event Story）——独立叙事层级

模拟宇宙里有"事件故事"（Event Story），这些是短篇叙事，描述星神和令使的历史事件，属于**高密度 lore**但分类上是"模拟宇宙内的推演故事"（L7 可信度）。

当前部分在 `activity` 和 `adventure` 文件中，但没有被标注为模拟宇宙内容，可信度处理错误。

#### 5.4 环境叙事（Environmental Storytelling）——完全未索引

- 地图上的标牌、告示、留言
- 地点描述文字
- NPC 背景服装/外观描述中的世界观暗示

这些内容获取难度高，但是玩家社区发现隐秘联系的重要来源之一。

---

### 六、系统架构层面

#### 6.1 Cascading Update Problem（级联更新问题）

当一个事实改变（可可利亚在 m1011503 身亡），受影响的数据应该同步更新：
- 可可利亚的 `current_status` → deceased
- 可可利亚的所有进行时关系 → `valid_until: m1011503`
- 布洛妮娅的接任关系 → `valid_from: m1011503`
- 所有对话场景中现在时态地称可可利亚为守护者的文本 → 需要标注时态上下文

**当前**：这些都是独立的手工操作，没有传播机制。未来重跑提取时会产生不一致。

#### 6.2 entities.json ↔ LightRAG 的实体漂移（Entity Drift）

- 我们的实体：`可可利亚`
- LightRAG 可能的节点：`可可利亚·兰德`、`守护者`、`大守护者可可利亚`

两个图中同一实体有不同表示，没有对应表，合并查询时会出错。需要一个 **entity resolution bridge**。

#### 6.3 多跳证据链的可信度衰减

当回答需要跨 N 个关系推理时，每一跳都有一定的不确定性。目前系统对 1-hop 和 4-hop 查询使用相同的置信度处理，没有衰减机制。

```
A is_related_to B (confirmed)
B is_related_to C (probable)
C is_related_to D (speculation)
→ A is_related_to D = speculation（取最低），但系统不会自动计算这个
```

---

### 七、语言和语义层面

#### 7.1 古典汉语典故层——星铁大量使用的隐性语义层

仙舟弧大量章节名使用《诗经》《楚辞》《山海经》的典故：
- 「金鼎灵树，穷途梼杌」— 梼杌是上古凶兽
- 「螣蛇无穴，旧梦亡阙」— 螣蛇是神话中的神兽
- 「帝弓司命」— 来自《山海经》中的神祇

这些典故在游戏内直接影射剧情含义（螣蛇无穴 = 幻胧的寄生状态），但当前系统无法识别这一语义层。这是中文特有的深层语义问题。

#### 7.2 同一概念的多语言变体

部分星神/概念有官方英文名，与中文音译不同：
- Ⅸ（虚无星神）在英文版有不同的翻译处理
- 「无名客」= Nameless (in English) - 含义有细微差别

当前 `aliases` 里没有官方英文名，跨语言查询时匹配会失败。

---

### 八、Meta-Narrative 层面（最复杂）

#### 8.1 游戏版本层次的 Canon 演化

不同版本的内容存在 Canon 权威性差异：
- 1.0 主线建立的设定 → 最基础 Canon
- 后续版本的续闻/终末任务 → 扩展 Canon，有时更正早期内容（大丽花案例）
- 限时活动故事 → 弱 Canon，活动结束后归档
- 模拟宇宙特殊关卡故事 → 非 Canon（明确标注为推演）

当前系统将所有版本内容平等处理，没有 Canon 层级标注。

#### 8.2 第四堵墙内容（Fourth Wall Content）

某些内容刻意打破叙事框架：
- 黑塔的某些对话带有自我意识（知道自己在某种模拟中）
- 某些周年活动内容是元叙事性质的
- 模拟宇宙角色有时会"意识到"自己是模拟

这类内容从 `reliability` 角度完全无法用现有框架处理——它既不是 `confirmed` 也不是 `fictional`，是独立的元叙事层级。

---

### 九、优先级更新（含本次新增）

| 缺口 | 影响 | 成本 | 优先级 |
|------|------|------|-------|
| 对话分支标注（1.1）| 全部对话数据质量 | 中 | **P0** |
| 命途与星神解耦（2.1）| Aeon 系统推理 | 低 | **P0** |
| 来源章节 vs 事件章节（上次）| 时间推理 | 低 | **P0** |
| 大丽花 retracted 标注（上次）| 匹诺康尼准确性 | 中（手工）| **P0** |
| 跨章节前后呼应结构（3.3）| 核心隐秘联系发现 | 高 | **P1** |
| 角色知识时效性（4.1）| 动态状态推理 | 高 | **P1** |
| 叙述视角标注（3.1）| 多方矛盾处理 | 中 | **P1** |
| 引用链（1.3）| 可信度精度 | 中 | **P2** |
| 角色发展弧（2.2）| 动态角色状态 | 高 | **P2** |
| 成就描述索引（5.2）| 隐性 lore 覆盖 | 低 | **P2** |
| entities.json ↔ LightRAG 同步（6.2）| 系统一致性 | 高 | **P2** |
| 多跳置信度衰减（6.3）| 推理质量 | 中 | **P2** |
| Canon 层级标注（8.1）| 版本间一致性 | 高 | **P3** |
| 宇宙地理结构（2.3）| 本体论完整性 | 极高 | **P3** |
| 角色间知识不对称图（上次）| 动机推理 | 极高 | **P3** |

---

## P5 — 社区整合 Lore 资源（记录在案，暂不提取）

> 来源：2026-06-08 联网调研

以下资源包含玩家社区对游戏 lore 的系统性整理，具备高度参考价值，但属于**非官方内容**，可信度应标注为 `legend`（社区共识，非官方 canon）：

| 资源 | 地址 | 说明 |
|------|------|------|
| Raven's Lore Guide | [Fandom 页面](https://honkai-star-rail.fandom.com/wiki/User:Raven.Familiar/Lore_Guides) | 综合 lore 指南，玩家社区最权威整理 |
| Lore FAQ | [rentry.co/srlorefaq](https://rentry.co/srlorefaq) | 常见 lore 问题与答案 |
| Navigators List | Google Docs 文档 | 无名客成员详细记录 |

**提取条件**：当系统对某类问题（如「无名客历史」「阿基维利失踪原因」）频繁无法回答时，考虑纳入此类内容，并在 chunk 元数据中明确标注 `reliability: legend, source_type: community_guide`。

**风险**：
- 社区内容可能包含玩家推断（B 类涌现推断），将其纳入会模糊「官方事实」和「玩家猜测」的边界
- 不同社区成员的解读可能互相矛盾

**状态**：待决策

